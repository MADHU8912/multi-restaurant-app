version: "3.9"

services:
  backend:
    build: ./backend
    container_name: multi-food-backend
    ports:
      - "5000:5000"

  frontend:
    build: ./frontend
    container_name: multi-food-frontend
    ports:
      - "8082:80"
    depends_on:
      - backend